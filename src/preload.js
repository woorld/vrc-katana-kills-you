// TODO: フロントからの機能オンオフ処理
// NOTE: サンドボックスが有効（nodeIntegration: false）な場合、preloadスクリプトはESModuleが使えない
// const { ipcRenderer, contextBridge } = require('electron');

// contextBridge.exposeInMainWorld('osc', {
//   onUpdateDeathCount: callback =>
//     ipcRenderer.on('update-death-count', (_, deathCount) => callback(deathCount)),
// });
console.log('dummy');
